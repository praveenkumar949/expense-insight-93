route:
  receiver: "default"
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h

receivers:
  - name: "default"
    # For convenience: logs alerts inside Alertmanager UI.
    # Add email/slack/webhook receivers here later.
    webhook_configs:
      - url: "http://localhost:1"

